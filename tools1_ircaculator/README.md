# BebeTools - 热像仪计算工具

## 项目介绍

BebeTools 是一个专业的热像仪参数计算工具，用于计算热像仪的关键性能参数。该工具提供了简洁直观的用户界面，帮助用户快速计算热像仪在特定条件下的分辨率和性能指标。

## 功能特点

- 🎯 **精确计算**：基于专业的光学计算公式
- 🖥️ **直观界面**：简洁易用的Web界面
- 📱 **响应式设计**：支持多种设备访问
- ⚡ **实时计算**：输入参数后即时显示结果
- 🔧 **专业工具**：面向热像仪技术人员和研究人员

## 功能说明

本工具主要用于计算热像仪的空间分辨率，通过输入设备参数和使用条件，自动计算出：

- 横向分辨率 (Rx)
- 纵向分辨率 (Ry)
- 基于距离的实际分辨率
- 系统的整体性能指标

## 使用方法

### 1. 访问工具

在浏览器中打开 `index.html` 文件，或通过Web服务器访问应用。

### 2. 输入参数

在界面中依次输入以下参数：

- **Rx (横向分辨率)**：热像仪传感器的横向像素数
- **Ry (纵向分辨率)**：热像仪传感器的纵向像素数
- **L (使用距离)**：热像仪到目标的距离，单位：米(m)
- **P (像元尺寸)**：传感器单个像素的物理尺寸，单位：微米(μm)
- **d (镜头焦距)**：热像仪镜头的焦距，单位：毫米(mm)

### 3. 执行计算

点击 **"计算"** 按钮，系统将自动计算并显示结果。

## 参数说明

### 输入参数详解

| 参数 | 名称 | 单位 | 说明 |
|------|------|------|------|
| Rx | 横向分辨率 | 像素 | 传感器水平方向的像素数量 |
| Ry | 纵向分辨率 | 像素 | 传感器垂直方向的像素数量 |
| L | 使用距离 | 米(m) | 热像仪与被测目标之间的距离 |
| P | 像元尺寸 | 微米(μm) | 单个像素的物理尺寸 |
| d | 镜头焦距 | 毫米(mm) | 热像仪光学系统的焦距 |

### 典型参数范围

- **分辨率**：常见范围 320×240 到 1024×768
- **使用距离**：1米 到 1000米
- **像元尺寸**：10μm 到 50μm
- **镜头焦距**：10mm 到 200mm

## 计算公式

### 空间分辨率计算

热像仪的空间分辨率主要通过以下公式计算：

```
瞬时视场角 (IFOV) = P / d
空间分辨率 = L × IFOV = L × (P / d)
```

其中：
- IFOV：瞬时视场角（弧度）
- P：像元尺寸（μm）
- d：镜头焦距（mm）
- L：使用距离（m）

### 视场角计算

```
水平视场角 = 2 × arctan(Rx × P / (2 × d))
垂直视场角 = 2 × arctan(Ry × P / (2 × d))
```

## 部署说明

本项目支持多种部署方式，从本地开发到生产环境部署，提供了完整的解决方案。

### 🚀 快速部署

#### 使用部署脚本（推荐）

**Linux/macOS:**
```bash
# 本地开发服务器
./deploy.sh local

# 部署到 Vercel
./deploy.sh vercel

# 部署到 Netlify
./deploy.sh netlify

# Docker 容器化部署
./deploy.sh docker
```

**Windows:**
```cmd
# 本地开发服务器
deploy.bat local

# 部署到 Vercel
deploy.bat vercel

# 部署到 Netlify
deploy.bat netlify

# Docker 容器化部署
deploy.bat docker
```

### 📋 部署方式详解

#### 1. 本地开发部署

**方式一：使用 Python HTTP 服务器**
```bash
# 进入项目目录
cd bebetools

# 启动服务器
python -m http.server 8080

# 访问地址
http://localhost:8080
```

**方式二：使用 npm 脚本**
```bash
# 安装依赖（可选）
npm install

# 启动开发服务器
npm start

# 或者
npm run serve
```

**方式三：使用 Node.js serve**
```bash
# 全局安装 serve
npm install -g serve

# 启动服务器
serve . -p 8080
```

#### 2. 静态网站托管部署

##### Vercel 部署

**自动部署（推荐）:**
```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录 Vercel
vercel login

# 部署项目
vercel --prod
```

**手动部署:**
1. 访问 [Vercel](https://vercel.com)
2. 连接 GitHub 仓库
3. 选择项目目录
4. 自动部署完成

##### Netlify 部署

**CLI 部署:**
```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录 Netlify
netlify login

# 部署项目
netlify deploy --prod --dir .
```

**拖拽部署:**
1. 访问 [Netlify](https://netlify.com)
2. 将项目文件夹拖拽到部署区域
3. 自动部署完成

##### GitHub Pages 部署

```bash
# 安装 gh-pages
npm install -g gh-pages

# 部署到 GitHub Pages
gh-pages -d .
```

#### 3. Docker 容器化部署

**构建镜像:**
```bash
# 构建 Docker 镜像
docker build -t bebetools:latest .

# 运行容器
docker run -d -p 8080:80 --name bebetools-container bebetools:latest

# 访问地址
http://localhost:8080
```

**使用 Docker Compose:**
```yaml
# docker-compose.yml
version: '3.8'
services:
  bebetools:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
```

```bash
# 启动服务
docker-compose up -d
```

#### 4. 传统服务器部署

**Apache 服务器:**
```bash
# 复制文件到 web 目录
sudo cp -r * /var/www/html/bebetools/

# 设置权限
sudo chown -R www-data:www-data /var/www/html/bebetools/
sudo chmod -R 755 /var/www/html/bebetools/
```

**Nginx 服务器:**
```nginx
# /etc/nginx/sites-available/bebetools
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/html/bebetools;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 🔧 部署配置文件

项目包含以下配置文件：

- **`package.json`** - npm 脚本和依赖管理
- **`vercel.json`** - Vercel 部署配置
- **`Dockerfile`** - Docker 容器配置
- **`nginx.conf`** - Nginx 服务器配置
- **`deploy.sh`** - Linux/macOS 部署脚本
- **`deploy.bat`** - Windows 部署脚本
- **`.gitignore`** - Git 忽略文件配置

### ✅ 部署验证

部署完成后，请验证以下功能：

1. **页面加载** - 确认页面正常显示
2. **资源文件** - 检查 CSS 和 JS 文件加载
3. **功能测试** - 测试计算功能是否正常
4. **响应式设计** - 在不同设备上测试

### 🐛 常见问题

**问题1：资源文件404错误**
```bash
# 检查文件路径
ls -la assets/

# 确认文件名大小写
# HTML中：index-bbbey6o2.js
# 实际文件：index-bbbey6o2.js
```

**问题2：Docker 容器无法访问**
```bash
# 检查容器状态
docker ps

# 检查端口映射
docker port bebetools-container

# 查看容器日志
docker logs bebetools-container
```

**问题3：权限问题**
```bash
# 设置执行权限
chmod +x deploy.sh

# 设置文件权限
chmod 644 *.html *.css *.js
```

### 📊 性能优化

**启用 Gzip 压缩:**
```nginx
gzip on;
gzip_types text/plain text/css application/javascript;
```

**设置缓存策略:**
```nginx
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

**CDN 加速:**
- 使用 Cloudflare 或其他 CDN 服务
- 配置静态资源缓存策略

## 技术栈

### 前端技术

- **React 18.3.1**：用户界面框架
- **Vite**：构建工具和开发服务器
- **Tailwind CSS**：样式框架
- **JavaScript/TypeScript**：核心逻辑实现

### 构建工具

- **Vite**：现代化的前端构建工具
- **ESBuild**：快速的JavaScript打包器
- **PostCSS**：CSS处理工具

### 开发环境

- **Node.js**：JavaScript运行环境
- **npm/pnpm**：包管理器

## 浏览器兼容性

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## 注意事项

1. **参数有效性**：请确保输入的参数在合理范围内
2. **单位转换**：注意各参数的单位要求
3. **精度限制**：计算结果受输入精度影响
4. **环境因素**：实际使用中还需考虑环境温度、湿度等因素

## 更新日志

### v1.0.0
- 初始版本发布
- 基础计算功能
- 响应式用户界面

## 许可证

本项目采用 MIT 许可证。详情请参阅 LICENSE 文件。

## 联系方式

如有问题或建议，请通过以下方式联系：

- 项目地址：[GitHub Repository]
- 技术支持：[Support Email]

---

*BebeTools - 让热像仪参数计算更简单*